{
   "manifest_version": 2,
   "content_scripts": [ {
      "exclude_globs": [  ],
      "exclude_matches": [  ],
      "include_globs": [ "https://gerrit.wikimedia.org/r/#/c/*" ],
      "js": [ "script.js" ],
      "matches": [ "http://*/*", "https://*/*" ],
      "run_at": "document_idle"
   } ],
   "converted_from_user_script": true,
   "description": "Adds a table near to the approval table in Wikimedia's Gerrit installation and can hide comments from jenkins-bot.",
   "name": "Gerrit Toggle CI",
   "version": "1"
}
